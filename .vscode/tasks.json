{
    "version":"2.0.0",
    "type": "shell",
    "tasks":[
        {
			"label": "build",
			"command": "make build DEBUG=1",
			"type": "shell",
			"options": {
				"cwd": "${workspaceFolder}"
			}
		}
	]
}